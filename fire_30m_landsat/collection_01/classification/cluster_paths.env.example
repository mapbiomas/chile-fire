# Copiar a cluster_paths.env (no se commitea; ver .gitignore)
#
# Plantillas leftraru:
#   cluster_paths.train.env.leftraru   → entrenamiento
#   cluster_paths.classify.env.leftraru → clasificación serie completa

export REPO_ROOT="${HOME}/fire"
export PYTHON="${HOME}/.conda/envs/mb_fuego/bin/python"
export MOSAIC_DIR="${HOME}/mosaics_cog"
export MODEL_DIR="${HOME}/models_col1"
export OUTPUT_DIR="${HOME}/classification_output"

# --- Clasificar UNA región (run_classify_region_slurm.sh) ---
# export REGION=r2
# export START_YEAR=2013
# export END_YEAR=2025
# unset MODEL_VERSION
# export AUTO_MODEL_VERSION_BY_YEAR=1
# sbatch --export=ALL classification/run_classify_region_slurm.sh

# --- Clasificar serie completa (run_classify_chile_slurm.sh) ---
# export REGIONS="r1 r2 r4 r6"
# sbatch --export=ALL classification/run_classify_chile_slurm.sh

# --- Entrenar UN modelo (run_train_fire_model_slurm.sh) ---
# export TRAIN_REGION=r2
# export TRAIN_VERSION=v1
# export SAMPLE_START_YEAR=2013
# export SAMPLE_END_YEAR=2018
# sbatch --export=ALL classification/run_train_fire_model_slurm.sh
